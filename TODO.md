# TODO / Backlog

Future ideas not yet implemented.

## application-tracker skill

A lightweight way to track applications across roles and companies.

- Log each application: company, role, date applied, which resume + cover-letter
  version was used (path in `output/`), JD link, and status
  (applied / screen / interview / offer / rejected / withdrawn).
- Storage: a simple `applications.md` table or `applications.csv` at the repo root
  (gitignore if it should stay private).
- Surface quick views: open applications, follow-ups due, per-company history.
- Useful when juggling many concurrent applications and resume variants.

Status: deferred (not needed right away).
